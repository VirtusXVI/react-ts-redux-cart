import React from 'react'
import styled from "styled-components";


export default function CartView() {
  return (
    <CartContainer>CartView</CartContainer>
  )
}

const CartContainer = styled.div`
  position: relative;
  top: 0;
  left: 0;
  background-color: rgba(0, 0, 0, 0.2);
`